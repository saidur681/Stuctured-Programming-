/*This c program is determine whether a number is Zero (0) or not.*/
#include<stdio.h>
int main(){
  float num;
  printf("Enter a number:");
    scanf("%f",&num);
  if(num==0){
    printf("The number is Zero(0) ");
  }else{
    printf("The number is not Zero(0)");
  }
return 0;
}
